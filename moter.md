# BLDC 三路 PWM 开环控制规划

**编写日期：** 2026-04-09  
**适用工程：** `CMAKEtest`  
**规划目标：** 在当前 `STM32F103ZET6 + HAL + FreeRTOS` 工程中，增加一个最小可运行的 BLDC 开环驱动能力，先实现“三路 PWM 输出 + 六步换相 + 启动对齐 + 开环加速 + 停机保护”的基础闭环前版本。

## 1. 当前工程现状

结合当前仓库内容，和电机控制直接相关的现状如下：

- MCU 为 `STM32F103ZET6`，封装为 `LQFP144`，可用引脚和定时器资源较充足。
- 当前工程已启用 `USART1`、`SPI2`、`CAN1`、`DMA`、`FreeRTOS`。
- 当前 `CubeMX` 配置中尚未启用任何 `TIM` 外设，也没有 PWM 输出配置。
- 当前 `Core/Inc/stm32f1xx_hal_conf.h` 中 `HAL_TIM_MODULE_ENABLED` 仍未打开。
- 当前主循环结构已经具备“初始化 + FreeRTOS 任务运行”的基本骨架，适合继续挂接一个电机控制模块。
- 当前仓库的代码组织方式比较清晰：
  - `unilib/src` 更适合放与具体板级资源解耦的通用控制逻辑
  - `Dev/port` 更适合放和当前板级定时器、GPIO、驱动器接线绑定的适配层

结论：当前工程适合新增 BLDC 模块，但第一步必须先补齐 `TIM/PWM` 外设基础。

## 2. 本次规划的边界

本次仅规划“简单开环 BLDC 驱动”，明确不包含以下内容：

- 不做霍尔闭环
- 不做编码器闭环
- 不做 FOC
- 不做电流环控制
- 不做自动识别参数
- 不做复杂保护联动策略

本阶段只解决以下最小目标：

- 可以输出三路 PWM
- 可以按六步换相表驱动三相
- 可以完成上电对齐
- 可以做简单开环启动和加速
- 可以设置一个基础占空比
- 可以安全停机并关闭输出

## 3. 关键硬件前提

“三路 PWM 控制三相占空比”这个目标能否直接成立，取决于功率驱动板的输入模式。

### 3.1 前提一：驱动板支持 `3-PWM` 输入模式

如果外部三相驱动板或栅极驱动芯片本身支持 `3-PWM` 输入模式，并且已经在硬件上处理好了以下问题，那么 MCU 只输出三路 PWM 是可行的：

- 上下桥驱动逻辑
- 必要死区
- 自举刷新
- 互锁保护

这种情况下，软件首版可以按“三路 PWM + 六步换相”推进。

### 3.2 前提二：如果功率级需要 MCU 直接管高低桥

如果当前硬件是离散三相半桥，且驱动器要求 MCU 分别提供高边和低边控制，那么只用三路普通 PWM 不够安全，也不够完整。此时应直接切换为：

- `6-PWM` 方案
- 互补输出
- 硬件死区
- 刹车输入或故障关断

这种情况下更适合使用高级定时器 `TIM1/TIM8`，而不是普通定时器的三路基础 PWM。

### 3.3 本文推荐假设

为了满足“先做一个简单开环版本”的目标，本文默认采用如下假设：

- 外部功率驱动级支持 `3-PWM` 输入，或已在硬件侧保证不会发生上下桥直通
- 首版优先验证换相逻辑、启动逻辑和基础调速逻辑
- 等首版跑通后，再决定是否升级到互补输出方案

## 4. 可选实现路径

### 方案 A：六步换相 + 三路 PWM + 外部驱动板处理桥臂逻辑

这是最贴合当前需求的首版方案。

特点：

- MCU 只需要输出三路 PWM
- 按六步换相表切换三相导通关系
- 通过统一设置 PWM 占空比控制平均驱动力
- 实现难度最低，最适合先打通最小可运行链路

优点：

- 改动最少
- 和当前工程的“先把基础链路接上”思路一致
- 容易尽快看到波形和电机转动结果

不足：

- 对驱动板输入模式有依赖
- 启动能力和抗负载能力有限
- 没有反馈时容易失步

### 方案 B：六步换相 + 互补 PWM + 死区控制

这是更工程化、更安全的下一阶段方案。

特点：

- MCU 直接控制高低桥逻辑
- 需要高级定时器、互补输出和死区配置
- 更适合后续扩展故障输入、刹车、闭环控制

优点：

- 更接近量产驱动思路
- 对桥臂时序可控性更强
- 后续升级空间更大

不足：

- 实现复杂度明显更高
- 首版开发成本更高
- 对定时器引脚分配要求更严格

### 方案 C：正弦 PWM 或 SVPWM 开环

这类方案在波形上更平滑，但不适合作为当前工程的第一阶段落地路径。

原因：

- 开环情况下即便波形更平滑，也仍然存在失步风险
- 软件复杂度明显增加
- 对定时器更新和数学计算要求更高

### 推荐选择

首版推荐采用 **方案 A**，但必须带一个决策门槛：

- 如果驱动板确实支持 `3-PWM` 安全输入模式，则继续方案 A
- 如果硬件并不支持，则不要勉强实现“三路 PWM”，应直接改走方案 B

## 5. 推荐控制流程

首版建议采用一个非常明确的开环状态机。

### 5.1 状态划分

- `STOP`：停止态，PWM 输出关闭
- `ALIGN`：对齐态，给固定相位和固定占空比，锁定转子初始位置
- `RAMP`：开环加速态，按照预设换相周期逐步加快
- `RUN`：运行态，以目标换相周期持续运行
- `FAULT`：故障态，立即关闭输出，等待人工清除

### 5.2 启动流程

建议的首版流程：

1. 上电后默认进入 `STOP`
2. 收到启动命令后，进入 `ALIGN`
3. 在固定扇区维持 `100 ms ~ 300 ms`
4. 进入 `RAMP`，从较慢的换相周期开始
5. 按预设斜坡逐步缩短换相周期
6. 达到目标周期后进入 `RUN`

### 5.3 停机流程

建议的首版流程：

1. 收到停止命令
2. 将目标占空比逐步拉低，或直接切到安全停机流程
3. 关闭三路 PWM 输出
4. 清空状态，回到 `STOP`

## 6. 换相策略建议

首版建议采用六步换相，不在第一阶段引入复杂调制。

### 6.1 六步换相基本思路

一个电角周期分成六个扇区，每个扇区只让两相参与驱动，另一相悬空。软件核心工作是：

- 根据当前扇区决定三相的导通关系
- 为参与驱动的相设置 PWM 占空比
- 按设定时间推进到下一个扇区

### 6.2 占空比控制建议

首版建议先用统一占空比，不做每相独立闭环修正：

- `duty` 作为全局驱动力参数
- `ALIGN`、`RAMP`、`RUN` 可分别设置不同默认占空比
- 通过限幅保证占空比不会过高

建议初始限制：

- 最小有效占空比：`5% ~ 10%`
- 启动占空比：`10% ~ 20%`
- 首版最大占空比先限制在 `50% ~ 60%`

## 7. 定时器与引脚规划建议

当前工程没有 PWM 外设占用，因此引脚选择空间较大。结合当前已占用资源：

- `PA9/PA10` 已被 `USART1` 使用
- `PA11/PA12` 已被 `CAN1` 使用
- `PB13/PB14/PB15` 已被 `SPI2` 使用
- `PB12` 已被普通 GPIO 使用

因此不建议首版再优先占用这些引脚。

### 7.1 推荐方案一：普通定时器三路 PWM

如果确认外部驱动板支持 `3-PWM` 模式，首版可优先考虑普通定时器的三路输出，例如：

- `TIM4 CH1/CH2/CH3`
- 或 `TIM3` 的三路通道

选择原则：

- 避开当前已占用引脚
- 尽量把三路 PWM 放在同一个定时器上
- 保证后续若要升级，也不会影响现有串口、SPI、CAN

### 7.2 推荐方案二：直接为后续升级预留高级定时器

如果硬件后续大概率会切到互补输出方案，建议一开始就考虑：

- `TIM1`
- 或 `TIM8`

这样即便首版暂时只用部分输出，后续升级也更平滑。

### 7.3 PWM 参数建议

首版建议参数如下：

- PWM 频率：`10 kHz ~ 20 kHz`
- 推荐先从 `20 kHz` 开始，兼顾听感和实现难度
- 定时器时钟：当前工程定时器时钟为 `72 MHz`

若采用边沿对齐 PWM，`20 kHz` 的一个简单参考配置为：

- `PSC = 0`
- `ARR = 3599`

这样便于后续直接按 `CCR` 改占空比。

## 8. 软件结构规划

结合当前工程已有目录风格，建议按“通用逻辑 + 板级适配”拆分。

### 8.1 通用控制层

建议新增：

- `unilib/src/bldc_openloop.h`
- `unilib/src/bldc_openloop.c`

职责建议：

- 定义电机状态机
- 保存目标占空比、当前扇区、换相周期
- 实现 `start/stop/set_duty/set_speed/tick` 等通用接口
- 生成当前扇区对应的三相信号请求

这一层尽量不直接依赖具体 `TIMx` 句柄和 GPIO 引脚。

### 8.2 板级端口层

建议新增：

- `Dev/port/bldc_port.h`
- `Dev/port/bldc_port.c`
- `Dev/port/bldc_config.h`

职责建议：

- 封装 PWM 启停
- 封装三相占空比设置
- 封装换相表映射到具体输出动作
- 提供使能脚、故障脚、刹车脚等板级接口
- 集中存放首版参数，如对齐时间、启动占空比、默认加速表

### 8.3 系统接入层

建议修改：

- `Core/Src/main.c`
- `Core/Src/freertos.c`
- `CMAKEtest.ioc`
- `Core/Inc/stm32f1xx_hal_conf.h`
- `CMakeLists.txt`
- `cmake/stm32cubemx/CMakeLists.txt`

职责建议：

- 在 `CubeMX` 中补齐 `TIM/PWM` 外设
- 打开 `HAL_TIM_MODULE_ENABLED`
- 将新增源文件加入构建
- 在系统初始化阶段完成 BLDC 模块初始化
- 根据最终策略决定是使用定时器中断还是任务轮询来推进换相状态机

## 9. 调度方式建议

首版不建议仅依赖 `osDelay(1)` 这种毫秒级任务延时去推进换相，因为它对高速运行场景过粗。

建议二选一：

### 方案 1：单独使用一个基础定时器中断推进换相

优点：

- 时间精度更稳定
- 更适合做开环斜坡
- 不容易受任务调度抖动影响

适合首版作为正式方案。

### 方案 2：先用任务轮询验证最小逻辑

优点：

- 改动少
- 初期更容易调试

不足：

- 高频换相能力弱
- 时间抖动更大

如果只是先让电机低速转起来，可以短期这样做，但不建议作为长期方案。

## 10. 建议的最小外部接口

为了便于后续接入 AT、CAN 或其他控制入口，建议先为电机控制模块定义简洁接口：

```c
void BLDC_OpenLoop_Init(void);
void BLDC_OpenLoop_Start(void);
void BLDC_OpenLoop_Stop(void);
void BLDC_OpenLoop_SetDuty(uint16_t duty_permille);
void BLDC_OpenLoop_SetPeriodUs(uint32_t period_us);
void BLDC_OpenLoop_Tick(void);
```

说明：

- `Init` 负责状态初始化
- `Start` 进入对齐和启动流程
- `Stop` 关闭输出并清状态
- `SetDuty` 统一使用千分比更方便做限幅
- `SetPeriodUs` 用于控制换相周期
- `Tick` 用于推进状态机

## 11. 风险与限制

本方案的主要风险如下：

- 没有位置反馈，启动时可能失步
- 负载变化大时，开环运行不稳定
- 如果功率驱动板并不真正支持 `3-PWM`，则存在硬件风险
- 如果没有电流保护、欠压保护、过温保护，首版调试风险较高
- 如果换相时序和占空比设置不合理，可能出现抖动、啸叫、发热

因此首版调试必须坚持以下原则：

- 先低占空比
- 先空载
- 先示波器看波形，再长时间上电
- 先验证单步换相正确，再验证自动加速

## 12. 分阶段实施建议

### 阶段 1：补齐外设基础

- 在 `CubeMX` 中启用目标定时器
- 配置三路 PWM 输出
- 重新生成初始化代码
- 打开 `HAL_TIM` 模块
- 完成构建接入

### 阶段 2：建立板级 PWM 端口层

- 完成 PWM 启停接口
- 完成三相占空比写入接口
- 完成电机使能和故障输入接口
- 先在不接电机的情况下验证三路 PWM 波形

### 阶段 3：实现开环控制状态机

- 实现 `STOP / ALIGN / RAMP / RUN / FAULT`
- 实现六步换相表
- 实现启动对齐和加速斜坡
- 完成基础命令接口

### 阶段 4：联调验证

- 空载启动
- 调整对齐时间
- 调整启动占空比
- 调整加速斜坡
- 观察能否稳定进入持续运行

## 13. 首版完成标准

满足以下条件即可认为首版目标达成：

- 上电后默认无 PWM 输出
- 启动命令后可以执行“对齐 -> 加速 -> 运行”
- 三路 PWM 波形与六步换相逻辑一致
- 停止命令后能够关闭输出
- 空载情况下，电机可重复启动并持续运行
- 代码结构没有把电机逻辑全部堆在 `main.c`

## 14. 本规划的最终建议

对于当前工程，最合理的落地顺序不是直接写大量控制代码，而是：

1. 先确认功率驱动板到底是不是安全的 `3-PWM` 输入模式
2. 再在 `CubeMX` 中补齐一个三路 PWM 定时器
3. 然后建立 `bldc_openloop + bldc_port` 两层结构
4. 最后只实现一个最小六步换相开环版本

如果这一步能稳定转起来，后续再考虑：

- 接入 `AT` 命令控制
- 接入 `CAN` 命令控制
- 增加故障输入
- 升级到霍尔闭环
- 升级到互补 PWM
- 升级到 FOC

这会比一开始就把目标铺得过大更稳妥。
