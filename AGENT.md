# 项目协作规则

`AGENT.md` 作为本工程的沟通核心要点文件，用于记录协作时必须遵守的重要规则。

## 当前重要规则

1. 对于不需要测试的流程，生成代码时忽略对应的测试方案、测试用例和测试说明。
2. 生成的代码注释和新增或修改的 Markdown（`.md`）文件统一使用中文。
3. 生成代码时需要为函数或方法添加注释，至少说明其功能以及各参数的含义。
4. 后续在本工程中默认不使用 `git worktree`，除非我明确提出需要使用；同时不再生成与 `worktree` 相关的说明、目录或流程内容。
5. 当前工程新增或修改的自有代码默认需要遵守 `MISRA C:2012` 规范；如因 HAL、FreeRTOS、CubeMX 或第三方代码边界无法完全满足，需要在实现中明确隔离并说明原因。
6. 新增或修改函数时，函数体内只允许保留一个 `return` 出口。
7. 涉及到计算部分，尽量使用对于单片机更加高效的算法，如位运算等来代替乘除